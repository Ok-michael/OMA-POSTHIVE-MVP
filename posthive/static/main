body {
  background-color: #f2f2f2; /* Light gray background */
  color: #2e3e50; /* Dark bluish-gray text */
  margin-top: 5rem; /* Maintain margin-top */
}

h1, h2, h3, h4, h5, h6 {
  color: #2e3e50; /* Dark bluish-gray headings */
}

.bg-steel {
  background-color: #e0e0e0; /* Lighter steel tone (can adjust if needed) */
}

/* Less dark green background for header content */
.site-header {
  background-color: #388E3C; /* Less dark green for the entire header */
  padding: 10px 20px;
  border: none; /* Remove border */
}

.site-header .navbar-nav {
  /* Removed background color for navbar links container */
  border-radius: 3px; /* Subtle rounded corners */
  padding: 5px 10px; /* Adjust padding for spacing */
}

.site-header .navbar-nav .nav-link {
  /* Inherit green color from the header */
  color: inherit; /* Inherits the green color from .site-header */
  font-weight: bold; /* Make links stand out */
  font-size: 1rem; /* Smaller font size */
  margin-right: 15px; /* Add spacing between links */
}

.site-header .navbar-nav .nav-link:hover {
  color: #e0e0e0; /* Slightly lighter on hover */
}

.site-header .navbar-nav .nav-link.active {
  font-weight: bolder; /* Emphasize active link */
}

.content-section {
  background-color: #f2f2f2; /* Light gray background for content sections */
  padding: 10px 20px;
  border: 1px solid #dddddd; /* Maintain border color */
  border-radius: 3px;
  margin-bottom: 20px;
}

.article-title {
  color: #2e3e50; /* Dark bluish-gray article title */
}

a.article-title:hover {
  color: #25D366; /* Green accent on hover */
  text-decoration: none;
}

.article-content {
  white-space: pre-line; /* Maintain pre-line behavior */
}

.article-img {
  height: 65px;
  width: 65px;
  margin-right: 16px;
}

.article-metadata {
  padding-bottom: 1px;
  margin-bottom: 4px;
  border-bottom: 1px solid #dddddd; /* Maintain border color */
}

.article-metadata a:hover {
  color: #2e3e50; /* Can keep the same for hover */
  text-decoration: none;
}

.article-svg {
  width: 25px;
  height: 25px;
  vertical-align: middle;
}

.account-img {
  height: 125px;
  width: 125px;
  margin-right: 20px;
  margin-bottom: 16px;
}

.account-heading {
  font-size: 2.5rem; /* Maintain font size */
}

/* Special for PostHive branding (green color) */
.site-header .navbar-brand {
  font-weight: bold;
  font-size: 1.2rem; /* Slightly larger font */
  color: #25D366; /* Green for PostHive */
  text-shadow: 1px 1px 1px rgba(0, 0, 0, 0.3); /* Subtle shadow for depth */
}

